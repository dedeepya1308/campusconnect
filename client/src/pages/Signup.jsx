import {useState}from'react';import{Link,useNavigate}from'react-router-dom';import api from '../services/api';import{useAuth}from'../context/AuthContext';import Notification from'../components/Notification';
export default function Signup(){const[form,setForm]=useState({name:'',email:'',password:'',role:'student'}),[error,setError]=useState('');const{login}=useAuth(),nav=useNavigate();const submit=async e=>{e.preventDefault();try{login((await api.post('/auth/signup',form)).data);nav('/student')}catch(e){setError(e.response?.data?.message||'Unable to register.')}};return <form onSubmit={submit} className="mx-auto max-w-md rounded-xl bg-white p-7 shadow"><h1 className="mb-5 text-2xl font-bold">Join CampusConnect</h1><Notification error={error}/>{[['name','Name','text'],['email','Email','email'],['password','Password','password']].map(([k,l,t])=><label key={k} className="mt-3 block">{l}<input required className="input mt-1" type={t} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label className="mt-3 block">I am a<select className="input mt-1" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="student">Student</option><option value="organizer">Organizer</option></select></label><button className="btn mt-6 w-full">Create account</button><p className="mt-4 text-sm">Already registered? <Link className="text-indigo-700" to="/login">Sign in</Link></p></form>}
