import { Link } from 'react-router-dom'; import { serverUrl } from '../services/api';
export default function EventCard({event}) { return <article className="overflow-hidden rounded-xl bg-white shadow"><div className="h-36 bg-gradient-to-br from-indigo-400 to-purple-500">{event.image && <img className="h-full w-full object-cover" src={serverUrl(event.image)} alt=""/>}</div><div className="p-4"><p className="text-sm font-medium text-indigo-600">{event.category}</p><h3 className="text-lg font-bold">{event.title}</h3><p className="mt-1 text-sm text-slate-600">{event.date} · {event.time} · {event.location}</p><p className="mt-2 line-clamp-2 text-sm text-slate-600">{event.description}</p><Link className="mt-4 inline-block font-medium text-indigo-700" to={`/events/${event.id}`}>View event →</Link></div></article> }
