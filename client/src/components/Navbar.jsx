import { Link, NavLink } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function Navbar(){ const {user,logout}=useAuth(); const dashboard = user?.role === 'admin' ? '/admin' : user?.role === 'organizer' ? '/organizer' : '/student'; return <header className="bg-indigo-700 text-white shadow"><nav className="mx-auto flex max-w-6xl items-center justify-between p-4"><Link className="text-xl font-bold" to="/">CampusConnect</Link><div className="flex items-center gap-4 text-sm"><NavLink to="/events">Events</NavLink>{user ? <><NavLink to={dashboard}>Dashboard</NavLink><NavLink to="/notifications">Notifications</NavLink><NavLink to="/profile">{user.name}</NavLink><button className="rounded bg-white/15 px-3 py-1" onClick={logout}>Sign out</button></> : <><NavLink to="/login">Login</NavLink><NavLink className="rounded bg-white px-3 py-1 text-indigo-700" to="/signup">Join now</NavLink></>}</div></nav></header> }
