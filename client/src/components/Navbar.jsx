import { Link, NavLink } from 'react-router-dom'; import { useEffect, useState } from 'react'; import { useAuth } from '../context/AuthContext'; import api from '../services/api';
export default function Navbar(){ const {user,logout}=useAuth(); const [unread,setUnread]=useState(0); const [dark,setDark]=useState(()=>localStorage.getItem('theme')==='dark'); const dashboard = user?.role === 'admin' ? '/admin' : user?.role === 'organizer' ? '/organizer' : '/student'; useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light')},[dark]); useEffect(()=>{if(!user)return setUnread(0);const load=()=>api.get('/notifications/unread-count').then(r=>setUnread(r.data.count)).catch(()=>{});load();const timer=setInterval(load,30000);return()=>clearInterval(timer)},[user]); return <header className="bg-indigo-700 text-white shadow"><nav className="mx-auto flex max-w-6xl items-center justify-between p-4"><Link className="text-xl font-bold" to="/">CampusConnect</Link><div className="flex items-center gap-4 text-sm"><NavLink to="/events">Events</NavLink><NavLink to="/clubs">Clubs</NavLink>{user && <><NavLink to={dashboard}>Dashboard</NavLink><NavLink className="relative" to="/notifications">Notifications{unread>0&&<span className="ml-1 inline-flex min-w-5 justify-center rounded-full bg-rose-500 px-1 text-xs text-white">{unread>99?'99+':unread}</span>}</NavLink></>}<button className="rounded bg-white/15 px-3 py-1" aria-label="Toggle color theme" title="Toggle color theme" onClick={()=>setDark(!dark)}>{dark?'☀ Light':'☾ Dark'}</button>{user ? <><NavLink to="/profile">{user.name}</NavLink><button className="rounded bg-white/15 px-3 py-1" onClick={logout}>Sign out</button></> : <><NavLink to="/login">Login</NavLink><NavLink className="rounded bg-white px-3 py-1 text-indigo-700" to="/signup">Join now</NavLink></>}</div></nav></header> }
